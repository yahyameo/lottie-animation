import { createAction } from '@reduxjs/toolkit';

// Action to add animation to the cache
export const addAnimationToCache = createAction<any>('animations/addAnimationToCache');

// Additional actions can be added here as needed
